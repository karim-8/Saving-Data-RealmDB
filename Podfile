# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Realm-DB' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Realm-DB
  pod 'RealmSwift'

  target 'Realm-DBTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Realm-DBUITests' do
    # Pods for testing
  end

end
